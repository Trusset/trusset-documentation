---
title: 'List Importable Order Books'
api: 'GET /orderbooks/stocks/api/order-books/importable'
description: 'Discover stock order books other instances have published'
---

Returns the stock order books that other instances have opened for import and that you have not imported yet. A book qualifies when it is `ACTIVE`, settles on-chain, has `allowImport` set, and belongs to another instance. Newest first, capped at 100 entries.

Only a summary is returned. Import the book to read its full record, its depth and its trades.

## Response Fields

<ResponseField name="data" type="array">
  <Expandable>
    <ResponseField name="id" type="string">Order book ID. Pass it to [Import Order Book](/endpoints/stock-trading/import-order-book).</ResponseField>
    <ResponseField name="tokenAddress" type="string">Base token.</ResponseField>
    <ResponseField name="quoteTokenAddress" type="string">Quote token.</ResponseField>
    <ResponseField name="name" type="string">Display name.</ResponseField>
    <ResponseField name="symbol" type="string">Display symbol.</ResponseField>
    <ResponseField name="tokenType" type="string">`STOCK_TOKEN`.</ResponseField>
    <ResponseField name="custodyContract" type="string">Custody contract the book settles through.</ResponseField>
    <ResponseField name="minOrderSize" type="string">Minimum quantity in order book units.</ResponseField>
    <ResponseField name="maxOrderSize" type="string">Maximum quantity, or `null`.</ResponseField>
    <ResponseField name="tickSize" type="string">Price increment in order book units.</ResponseField>
    <ResponseField name="makerFeeBps" type="integer">Maker fee charged by the owning book.</ResponseField>
    <ResponseField name="takerFeeBps" type="integer">Taker fee charged by the owning book.</ResponseField>
    <ResponseField name="createdAt" type="string">ISO 8601 timestamp.</ResponseField>
  </Expandable>
</ResponseField>

<Note>
  The list is not filtered by network. [Import Order Book](/endpoints/stock-trading/import-order-book) refuses a book whose owner runs on a different network with `NETWORK_MISMATCH`.
</Note>

<RequestExample>
```bash cURL
curl "https://api.trusset.org/orderbooks/stocks/api/order-books/importable" \
  -H "X-API-Key: trusset_your_key_here"
```
```typescript TypeScript
const res = await fetch(
  'https://api.trusset.org/orderbooks/stocks/api/order-books/importable',
  { headers: { 'X-API-Key': 'trusset_your_key_here' } }
);
const { data } = await res.json();
for (const book of data) console.log(book.symbol, book.id, book.takerFeeBps);
```
</RequestExample>

<ResponseExample>
```json Response
{
  "success": true,
  "data": [
    {
      "id": "clx_ob_stock_044",
      "tokenAddress": "0x51f2b9d0e77c4a1b83ce6d4a9271e5f3a0c8b912",
      "quoteTokenAddress": "0x98ad0ca091552e23c564b41c74282e5343d03e8a",
      "name": "Helvetia Growth",
      "symbol": "HELG",
      "tokenType": "STOCK_TOKEN",
      "custodyContract": "0x98c33b10823172e62a31d6048773df1054c4f821",
      "minOrderSize": "1000000",
      "maxOrderSize": null,
      "tickSize": "10000",
      "makerFeeBps": 0,
      "takerFeeBps": 10,
      "createdAt": "2025-05-20T14:12:00.000Z"
    }
  ]
}
```
</ResponseExample>

## Error Codes

| Code | HTTP | Cause |
|---|---|---|
| `SERVICE_NOT_ENABLED` | `403` | The Trading service is not enabled on this instance |
