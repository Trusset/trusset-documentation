extends: existence
message: "Explanation goes in the prose above the block, not in a comment inside it. Trailing labels on positional arguments are exempt."
level: warning
scope: raw
tokens:
  - '(?m)^\s*//\s*\S'
